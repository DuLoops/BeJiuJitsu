// import express from 'express';
// import { UserSkillController } from '../controllers/userSkill.controller';

// const router = express.Router();
// const userSkillController = new UserSkillController();

// router.post('/', (req, res) => userSkillController.createUserSkill(req, res));
// router.get('/', (req, res) => userSkillController.getUserSkills(req, res));
// router.put('/:id', (req, res) => userSkillController.updateUserSkill(req, res));
// router.post('/:id/usage', (req, res) => userSkillController.addSkillUsage(req, res));
// router.delete('/:id', (req, res) => userSkillController.deleteUserSkill(req, res));

// export default router;
